<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ifxme.tvos">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".mvp.common.TVApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/title_activity_login"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.ui.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.ui.activity.task.AddTaskActivity" />
        <activity android:name=".mvp.ui.activity.MainActivity" />
        <activity
            android:name=".mvp.ui.activity.contact.ContactActivity"
            android:label="@string/title_activity_contact" />
        <activity
            android:name=".mvp.ui.activity.task.TaskActivity"
            android:label="@string/title_activity_task" />
        <activity android:name=".mvp.ui.activity.task.TaskDetailActivity" />
        <activity
            android:name=".mvp.ui.activity.NavigationActivity"
            android:label="@string/app_name"></activity>
    </application>

</manifest>